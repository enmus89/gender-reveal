# URL of the deployed Google Apps Script web app (ends in /exec).
# Local dev: copy this file to .env and fill it in.
# Production: set the same value as an Actions *Variable* named VITE_SHEET_API_URL
#   (repo Settings -> Secrets and variables -> Actions -> Variables).
VITE_SHEET_API_URL=""
